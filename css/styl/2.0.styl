h2
  @media forMobile
    font-size 1.2rem

.loading, .loading:after
  border-radius 50%
  width 10em
  height 10em

.loading
  display block
  margin 120px auto 0 auto
  font-size 10px
  position relative
  text-indent -9999em
  border-top 1.1em solid rgba(255, 255, 255, 0.2)
  border-right 1.1em solid rgba(255, 255, 255, 0.2)
  border-bottom 1.1em solid rgba(255, 255, 255, 0.2)
  border-left 1.1em solid #EEE
  transform translateZ(0)
  animation load8 1.1s infinite linear

@keyframes load8
  0%
    -webkit-transform rotate(0deg)
    transform rotate(0deg)

  100%
    -webkit-transform rotate(360deg)
    transform rotate(360deg)

@keyframes load8
  0%
    -webkit-transform rotate(0deg)
    transform rotate(0deg)

  100%
    -webkit-transform rotate(360deg)
    transform rotate(360deg)

.sidebar.width-25rem
  @media forMobile
    width 100%

.drawer-block
  h1
    margin-bottom 2rem

.article .article-body
  @media forMedium
    padding 3rem

  @media forMobile
    padding 2rem 1rem

// ul.menu > li > a,
// ul.menu > li > .item
// display: block
.prettyprint
  border 0 !important
  overflow auto
  background var(--background-color-blockquote)
  color var(--text-color-strong)
  border-radius 0.25rem
  padding 0.4rem !important

  code
    width 0
    display block
    padding 0

.list > .item a { position: relative; }
.list > .item a .t-project-icon { position: absolute; top: 2.75rem; left: 2.75rem; border-radius: 0.125rem; padding: 0rem; ba1ckground: var(--white); }

.badge
  white-space nowrap
